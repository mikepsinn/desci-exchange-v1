const Logo = ({ dark = false }) => {
  return dark ? (
    <img
      src={'/images/desci-exchange/desci-exchange-text-logo-wide.png'}
      width={300}
      height={80}
      alt="DESCI Exchange"
    />
  ) : (
    <img
      src={'/images/desci-exchange/desci-exchange-text-logo-wide.png'}
      width={300}
      height={80}
      alt="DESCI Exchange"
    />
  );
};

export default Logo;
