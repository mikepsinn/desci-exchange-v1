import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'connect',
  webDir: '../../dist/packages/connect',
  bundledWebRuntime: false,
};

export default config;
